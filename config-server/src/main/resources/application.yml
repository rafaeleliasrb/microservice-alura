server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rafaeleliasrb/microservice-alura
          search-paths: configuration/fornecedor, configuration/loja
